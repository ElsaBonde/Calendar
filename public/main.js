window.addEventListener('DOMContentLoaded', main);

function main() {
  todaysDate();
  updateClock();
  setInterval(updateClock, 60000);
  // Init today view
  // Init todos list
  // Init calendar
}

