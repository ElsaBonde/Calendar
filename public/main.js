window.addEventListener('DOMContentLoaded', main);

function main() {
  todaysDate();
  updateClock();
  setInterval(updateClock, 60000);
  console.log('Hi students!');
  // Init today view
  // Init todos list
  // Init calendar
}

